## Summary

What does this change and why?

## Related issue

Closes #

## Checklist

- [ ] Ran `python3 -m unittest discover -s tests`
- [ ] Ran `python3 scripts/validate_gate_artifacts.py`
- [ ] Updated docs / fixtures / golden contracts if behavior or `--json` output changed
- [ ] Rebuilt the dashboard bundle if `frontend/` changed (`npm run build`)
- [ ] Preserves the proposal / check / replay / policy / locks safety gate

## Notes

Anything reviewers should know (trade-offs, follow-ups, out-of-scope items).
